#include "default.h"

/*************************************************************************************************/

FileFormat formatDefault = {
	"Default",
	FILE_TYPE_UNKNOWN,
	ICON_CUSTOM_FILE,
	NULL,
	NULL
};